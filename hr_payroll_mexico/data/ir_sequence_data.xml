<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

    <!-- Sequences for hr.payroll.period ordinary -->
    <record id="seq_hr_payroll_period_or" model="ir.sequence">
        <field name="name">Period Ordinary</field>
        <field name="code">hr.payroll.period.or</field>
        <field name="prefix">MO</field>
        <field name="padding">3</field>
        <field name="company_id" eval="False"/>
    </record>

    <!-- Sequences for hr.payroll.period settlement -->
    <record id="seq_hr_payroll_period_se" model="ir.sequence">
        <field name="name">Period Settlement</field>
        <field name="code">hr.payroll.period.se</field>
        <field name="prefix">MF</field>
        <field name="padding">3</field>
        <field name="company_id" eval="False"/>
    </record>

    <!-- Sequences for hr.payroll.period extraordinary -->
    <record id="seq_hr_payroll_period_ex" model="ir.sequence">
        <field name="name">Period Extraordinary</field>
        <field name="code">hr.payroll.period.ex</field>
        <field name="prefix">ME</field>
        <field name="padding">3</field>
        <field name="company_id" eval="False"/>
    </record>

    </data>
</odoo>